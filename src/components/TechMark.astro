---
interface Props { size?: number; label?: string; }
const { size = 28, label = '' } = Astro.props;
---
<svg class="tech-mark" width={size} height={size} viewBox="0 0 32 32" fill="none" aria-hidden={label ? undefined : 'true'} role={label ? 'img' : undefined} aria-label={label || undefined}>
  <circle cx="16" cy="16" r="11.5" stroke="currentColor" stroke-width="1.5"/>
  <circle cx="16" cy="16" r="4" stroke="currentColor" stroke-width="1.5"/>
  <path d="M16 1v8M16 23v8M1 16h8M23 16h8M6.1 6.1l4 4M21.9 21.9l4 4" stroke="currentColor" stroke-width="1.5"/>
</svg>
